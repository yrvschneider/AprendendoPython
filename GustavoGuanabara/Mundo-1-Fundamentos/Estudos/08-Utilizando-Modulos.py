# Vendo como fazer a importação de uma biblioteca, estamos usando a biblioteca math.
# Vimos que podemos utilizar o modo de (import math), para importar toda a lista da biblioteca math.
# tambem podemos utilizar (from math import sqrt), para importar somente a função sqrt para ser utilizada.
# podemos utlizar o (from math import sqrt, floor), agora podemos utilizar o floor tambem na nossa programação. Se precisar de outra funçao, só utitilizarmos a virgula para separar e o nome da função.

# import math
# from math import sqrt
# num = int(input('Digite um número: '))
# raiz = sqrt(num)
# print(raiz)

import random
num = random.random()
num1 = random.randint(1, 10)
print(num)
print(num1)